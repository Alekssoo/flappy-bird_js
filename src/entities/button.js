import Entity from "./base.js";

export default class Button extends Entity {
    constructor(params) {
        super (params)
    }

    draw() {
        super.draw()
    }

     update() {
        
     }
 
}