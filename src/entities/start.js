import Entity from "./base.js";

export default class Start extends Entity {
    constructor(params) {
        super (params)
    }

    draw() {
        //отрисовываем стартовый экран
        super.draw()

    }

}